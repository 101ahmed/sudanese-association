# Locale & translator (single place; Flex recipe default is "en" — overridden here)
framework:
    default_locale: ar
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks: [en, fr]
