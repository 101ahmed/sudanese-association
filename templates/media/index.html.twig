{% extends 'base.html.twig' %}

{% block title %}{{ 'media.title'|trans }}{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">{{ 'media.title'|trans }}</h1>
        <p class="text-center text-muted mb-0">{{ 'media.text'|trans }}</p>
    </div>
{% endblock %}
