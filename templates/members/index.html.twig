{% extends 'base.html.twig' %}

{% block title %}{{ 'members.title'|trans }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">{{ 'members.title'|trans }}</h1>
    <p class="text-center text-muted mb-0">{{ 'members.text'|trans }}</p>
</div>
{% endblock %}
